import React from 'react';
import Header from "./Header";
import Routing from "./Routing";

const Main: React.FC = () => {
    return (
        <div>
            <Header/>
            <Routing/>
        </div>
    );
};

export default Main;